import Sidebar from "@/layouts/Sidebar";
import React from "react";

const page = () => {
    return (
        <div>
            <section className="flex">
                <Sidebar />
                rental
            </section>
        </div>
    );
};

export default page;
