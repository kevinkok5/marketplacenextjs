import Sidebar from "@/layouts/Sidebar";
import React from "react";

const page = () => {
    return (
        <div>
            <section className="flex">
                <Sidebar />
            </section>
        </div>
    );
};

export default page;
